# 🗂 会话工作日志 2026-09-25

> 时间一律 UTC。密钥只保留前 4 位。

## 时间线

| 时间 (UTC) | 操作 | 结果 |
|-----------|------|------|
| 02:27 | 用户要求"用完礼金额度" → 改为有价值的任务：日常巡检、生态扩充、GitHub 热点 | 方案确认 |
| 02:29 | 创建 3 个 Routines（热点日报 / 仓库巡检 / 生态周报） | 2 个被权限系统拦截；1 个建成但没挂仓库，后已删除 |
| 02:3x | 手动运行一次热点简报、巡检、生态评估 | 产出 3 份报告（见 `reports/`） |
| 02:3x | 巡检发现旧仓库 `wjhwjh666/---` 里有 6 个 `sk-` 密钥和 1 个 `github_pat_` | 已上报 |
| 02:42 | 旧仓库开 PR #1（报告、ruff 修复、生态表更新） | 随旧仓库删除 |
| 02:45 | 删除 `proxy_server.py` 里写死的 `sk-3777****` 默认值 | 改为只读环境变量 `RIGHTCODE_API_KEY` |
| 02:4x | 尝试删除会话记录里的密钥 | 被拦截（会话记录篡改），改由用户在本机处理 |
| 02:5x | 用户在本机清理 26 个日志、快照、缓存文件里的密钥 | 完成；凭据文件、二进制、数据库按建议不动 |
| 03:0x | 用户删除 `wjhwjh666/---` | 已确认删除 |
| 03:09 | `dotfiles` 定为唯一存储仓库：加 `CLAUDE.md` 声明、`.gitignore`，迁入报告 | 旧 dotfiles 开 PR #3 |
| 03:2x | 排查时区与位置暴露 | 发现 dotfiles 12 个历史提交带非 UTC 时区偏移、`signed-test.txt` 带时区、路径含真实姓名 |
| 03:25 | 实测提交流程 | 发现两个缺陷：扫描不拦截提交；扫描规则会匹配到自身文本 |
| 03:26 | 新增 `scripts/hooks/pre-commit` | 5 个用例全部符合预期 |
| 03:3x | 删除容器里已删除仓库的残留副本（35MB） | 完成 |
| 03:40 | 用户删库重建 dotfiles；推送 1 个干净的 UTC 提交 `3f6fda6` | 完成，远程只有 master |
| 03:5x | 本机：设置 `TZ=UTC0`，clone 新仓库，启用钩子 | `git var` 输出为 `+0000`，验证通过 |

## 经验教训

1. **会话记录不能进 git**：`.claude/projects/*.jsonl` 里是对话原文，粘贴过的密钥会原样留在里面。`.gitignore` 现已拦截 `projects/`、`*.jsonl`、`file-history/`。
2. **`.gitignore` 规则要覆盖子目录**：`projects/*.jsonl` 只匹配第一层，要写成 `projects/`。
3. **扫描命令不能只打印结果**：结果不为 0 时照样能提交，等于没扫。现在改由 pre-commit 钩子强制拦截。
4. **正则写进文档会匹配到自身**：规则要写成不会匹配自身文本的形式，比如用字符类包住斜杠（`[/]`）。这份日志第一次提交时，这条写的是字面量，结果被钩子拦下。命令链也要用 `&&` 连接，提交失败就不推送。
5. **提交元数据会泄露时区**：git 会记录作者所在时区的偏移。本机设置 `TZ=UTC0`，钩子也会检查提交时区。
6. **凭据文件、二进制、数据库不能按文本替换**：`.credentials.json`、`auth.json`、程序本体、`*.pyc`、`*.db` 被文本方式写回会损坏。要让旧密钥失效，靠的是吊销和更换。

## 待办

- [ ] 中转站后台吊销 6 个旧 key 并更换（`sk-a052****`、`sk-3777****`、`sk-Q5TH****`、`sk-TpJY****`、`sk-aHRc****`、`sk-qVcg****`）
- [ ] 确认历史中出现过的 `github_pat_` 已在 GitHub 吊销
- [ ] 确认 `~/dotfiles.old` 没有要保留的内容后删除
- [ ] 本机 `~/.claude/CLAUDE.md` 的 Git 规则改为指向 dotfiles
- [ ] 在 claude.ai 的 Routines 页面手动创建 3 个定时任务（选仓库 `wjhwjh666/dotfiles`，勾选 GitHub 连接器）
