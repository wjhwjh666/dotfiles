# 🔍 仓库巡检 2026-09-25：2 项问题

> 仓库可见性：**private**（已通过 GitHub 集成确认）。本报告中的所有密钥均已打码，只保留前 4 位。

## 结果总览

| # | 严重级别 | 检查项 | 结果 |
|---|---------|--------|------|
| 1 | 🔴 高 | 密钥泄露 | 被跟踪的文件里有 6 个不同的 `sk-` 密钥，git 历史中也有 |
| 2 | 🟡 中 | `.gitignore` 规则漏洞 | `.claude/projects/*.jsonl` 只匹配第一层，子目录里的会话记录没被忽略；41 个会话文件已被跟踪 |
| 3 | 🟢 通过 | 敏感路径 | `.env` / `*.pem` / `*.key` / `settings.json` / `.openclaw/` / `.config/` 都已忽略，且都未被跟踪 |
| 4 | 🟢 通过 | JSON 合法性 | 5 个 JSON 全部合法（已排除 `.paddlex/`） |
| 5 | 🟢 已修复 | ruff F 类错误 | 12 处自动修复（F401 未使用导入、F541 无占位符 f-string）；剩余 1 处 F841 和 E501 行过长未改 |
| 6 | 🟢 通过 | CLAUDE.md | 非空（26,829 字节），包含"CEO 交付标准"段 |

## 1. 密钥泄露明细

| 前缀 | 长度 | 所在位置 | 推测用途 | 能否用 |
|------|------|---------|---------|--------|
| `sk-a052****` | 35 | 8 个 `.claude/projects/**.jsonl` 会话记录 | DeepSeek API Key（上下文中出现过 `deepSeekApiKey` 和 `ANTHROPIC_AUTH_TOKEN`） | ⬜ 未验证 |
| `sk-3777****` | 35 | `.claude/proxy_server.py`，作为 `RIGHTCODE_API_KEY` 的**默认值写死在代码里** | RightCode 中转 | ⬜ 未验证 |
| `sk-Q5TH****` | 51 | 1 个会话记录（`display` 字段，用户粘贴的输入） | 未知 | ⬜ 未验证 |
| `sk-TpJY****` | 51 | 同上 | 未知 | ⬜ 未验证 |
| `sk-aHRc****` | 51 | 同上 | 未知 | ⬜ 未验证 |
| `sk-qVcg****` | 51 | 同上 | 未知 | ⬜ 未验证 |

git 历史：提交 `2d5317e`（chore: init baseline）引入了 `sk-` 和 `github_pat_` 两类密钥，`b9cbd2c` 中也有 `sk-`。

**为什么没删**：用户要求"不工作的密钥才删"。但本环境连不上 `api.deepseek.com`、`openrouter.ai`、`api.openai.com`（curl 返回 000，被网络策略拦截），无法判断密钥是否有效，所以一个都没删。

### 建议（需要用户本人操作）

1. 到各服务商后台查看或吊销这 6 个密钥。凡是出现在文件里的，不管还能不能用，都建议**轮换**。
2. 确认已作废后，可以让 Claude 做两件事：
   - 把 `proxy_server.py` 里的默认值改成 `""`，只从环境变量读取；
   - 把 `.gitignore` 规则改为 `.claude/projects/`，并执行 `git rm -r --cached .claude/projects`（只取消跟踪，本地文件保留）。
3. git 历史里的密钥需要用 `git filter-repo` 清理。这会改写历史，必须由用户决定，定时任务不会自动执行。
4. 历史中出现过 `github_pat_`，建议到 GitHub → Settings → Developer settings 确认该 Token 已吊销。

## 5. ruff 修复明细

| 文件 | 修复 |
|------|------|
| `.local/bin/ai_weekly_report.py` | 删除未使用的 `import urllib.request` |
| `.local/bin/check_deepseek_balance.py` | 删除未使用的 `import time` |
| `.local/bin/eco_scanner.py` | 删除未使用的 `sys` 和 `urllib.request`；7 处无占位符的 `f"..."` 改为普通字符串 |
| `.local/bin/preprocess.py` | 删除未使用的 `import shutil` |

验证：`ruff check --select F401,F541` → 0 remaining；`python3 -m py_compile .local/bin/*.py` 通过。
